# -----------------
# Server (backend)
# -----------------
PORT=4000
NODE_ENV=development
# Example MongoDB connection string. Replace with your credentials/cluster/dbname.
# For a local MongoDB instance use: mongodb://localhost:27017/slipwise
DATABASE_URL=mongodb://localhost:27017/slipwise
OCR_LANG=eng

# -----------------
# Mobile (Expo) / Client
# -----------------
# Base API url the mobile app should call. When running on a device, replace
# localhost with your machine IP (e.g. http://192.168.1.10:4000) or use a tunnel.
API_URL=http://localhost:4000

# Notes:
# - Copy this file to `.env` in the root or to `server/.env` and `mobile/.env` as needed.
# - Keep secrets (production DB credentials, API keys) out of source control.
